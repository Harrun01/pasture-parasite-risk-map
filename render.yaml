services:
  - type: web
    name: parasite-risk-api
    runtime: python
    rootDir: ./weatherAPI
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn weather_api:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0